{
  "name": "@sketcherson/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup src/index.ts src/domain/drawingSnapshotWorker.ts --format esm --out-dir dist --clean",
    "test": "vitest run"
  },
  "dependencies": {
    "@sketcherson/common": "*",
    "@sketcherson/demo-game": "*",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "socket.io-client": "^4.8.1",
    "tsup": "^8.5.0",
    "tsx": "^4.19.4",
    "vitest": "^3.1.2"
  }
}
